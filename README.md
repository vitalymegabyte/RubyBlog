# README

This is simple blog project. I added slugs, that can work. Minimum length of slug is 5, maximum length of slug is 30. Blog does not generate slugs automatically, but checks that you have added it while you were writing post. Name is "dhh", password is "secret".
